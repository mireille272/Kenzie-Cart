export const API_URL = import.meta.env.VITE_API_URL || 'api'

export const CATEGORIES = ['electronics', 'accessories', 'decor', 'wellness']

export const NODE_ENV = import.meta.env.VITE_NODE_ENV || 'development'
